name: Dev-Adhoc
run-name: triggered manually by ${{ github.actor }}
on: 
  workflow_dispatch:
jobs: 
  call-workflow-passing-data:
    permissions:
      contents: read
      pull-requests: write
    uses: ./.github/workflows/no-trigger.yml
    with:
      lane: fastlane increase_build_number